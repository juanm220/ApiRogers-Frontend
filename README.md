# Hawking Quick Service — Frontend (Prototype)
React app (MERN) — Dashboard, Admin y UI consistente (paleta fría).

## Scripts
- `npm install`
- `npm run dev` (Vite) o `npm start` (CRA)
- `npm run build`

## Env
Copiar `.env.example` a `.env`.
